rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // All access goes through server-side route handlers (admin SDK).
    // No direct client reads/writes.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
